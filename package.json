{
  "name": "@tilecloud/mbgl-export-control",
  "version": "0.3.3",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "start": "npm run build && cd docs && http-server -o",
    "watch": "parcel -d ${TMPDIR} example/index.html --open",
    "build": "rm -fr docs && parcel build -d docs/ example/index.html --public-url '.' && touch docs/.nojekyll",
    "test": "eslint \"src/**/*.js\""
  },
  "author": "Takayuki Miyauchi",
  "license": "MIT",
  "devDependencies": {
    "@mapbox/mapbox-gl-draw": "^1.0.9",
    "@tilecloud/mbgl-fork-me-control": "^0.2.0",
    "@tilecloud/mbgl-tilecloud-control": "^0.3.0",
    "eslint": "^5.4.0",
    "http-server": "^0.11.1",
    "parcel": "^1.9.7"
  },
  "dependencies": {
    "canvas-toBlob": "^1.0.0",
    "file-saver": "^1.3.8"
  }
}
