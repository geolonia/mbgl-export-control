{
  "name": "@tilecloud/mbgl-export-control",
  "version": "0.3.2",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "start": "npm run build && http-server $(pwd)/docs -o",
    "build": "parcel build src/app.js && parcel build -d docs/dist docs/src/app.js --no-cache",
    "test": "eslint src/*.js"
  },
  "author": "Takayuki Miyauchi",
  "license": "MIT",
  "devDependencies": {
    "@mapbox/mapbox-gl-draw": "^1.0.9",
    "@tilecloud/mbgl-tilecloud-control": "^0.1.0",
    "http-server": "^0.11.1",
    "parcel": "^1.9.7"
  },
  "dependencies": {
    "canvas-toBlob": "^1.0.0",
    "file-saver": "^1.3.8"
  }
}
