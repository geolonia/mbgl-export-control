{
  "name": "@tilecloud/mbgl-export-control",
  "version": "0.3.2",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "start": "npm test && npm run build && http-server $(pwd)/docs -o",
    "build": "npm test && parcel build src/app.js && parcel build -d docs/dist docs/src/app.js --no-cache",
    "test": "eslint \"src/**/*.js\""
  },
  "author": "Takayuki Miyauchi",
  "license": "MIT",
  "devDependencies": {
    "@mapbox/mapbox-gl-draw": "^1.0.9",
    "@tilecloud/mbgl-fork-me-control": "^0.1.0",
    "@tilecloud/mbgl-tilecloud-control": "^0.2.0",
    "eslint": "^5.3.0",
    "http-server": "^0.11.1",
    "parcel": "^1.9.7"
  },
  "dependencies": {
    "canvas-toBlob": "^1.0.0",
    "file-saver": "^1.3.8"
  }
}
